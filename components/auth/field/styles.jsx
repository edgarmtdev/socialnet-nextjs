import styled from "styled-components"
import FormField from "."

export const FieldStyled = styled(FormField)`
    padding: 5px;
    outline: none;
    font-size: 14px;
    border-radius: 2px;
    color: white;
`